---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - src/app/auth/login/page.tsx
  - src/app/auth/register/page.tsx
  - src/app/auth/reset-password/page.tsx
  - src/app/auth/layout.tsx
  - src/components/auth/LoginForm.tsx
  - src/components/auth/RegisterWizard.tsx
  - src/components/auth/ResetPasswordForm.tsx
  - src/components/ui/Input.tsx
  - src/components/ui/Button.tsx
  - src/components/ui/ErrorMessage.tsx
  - src/hooks/useAuth.ts
  - src/lib/api-client.ts
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
must_haves:
  truths:
    - Registration uses step-by-step wizard (email → password → confirmation)
    - Forms validate on submission (not real-time)
    - Design is minimalist (white background, clean inputs)
    - Error messages appear below each field
    - Login form is single-step with email/password
    - Password reset has request and confirm pages
  artifacts:
    - path: "src/components/auth/RegisterWizard.tsx"
      provides: "3-step registration wizard component"
    - path: "src/components/auth/LoginForm.tsx"
      provides: "Login form component"
    - path: "src/components/auth/ResetPasswordForm.tsx"
      provides: "Password reset request and confirm forms"
    - path: "src/hooks/useAuth.ts"
      provides: "Auth state management hook"
    - path: "src/lib/api-client.ts"
      provides: "API client with error handling"
  key_links:
    - from: "RegisterWizard"
      to: "/api/auth/register"
      via: "api-client.ts"
    - from: "LoginForm"
      to: "/api/auth/login"
      via: "api-client.ts"
    - from: "useAuth"
      to: "/api/auth/me"
      via: "useEffect on mount"
---

<objective>
Create the authentication frontend: step-by-step registration wizard, login form, and password reset pages. Implement minimalist design per UX decisions (Google/Linear style, field-level errors, validation on submit).

Purpose: Frontend is the user-facing interface for auth. The wizard flow and minimalist design are core UX differentiators mentioned in CONTEXT.md.
Output: Complete auth UI with all forms, proper error handling, and integration with backend API.
</objective>

<execution_context>
@C:/Users/Dev/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-03-PLAN.md
@.planning/phases/01-foundation/01-CONTEXT.md

## Dependencies
Requires 01-03 (API endpoints) to be complete.

## UX Decisions (LOCKED from CONTEXT.md)
- Wizard step-by-step: email → password → confirmation
- Validation at submission (not real-time)
- Design: minimalist, white, clean (Google/Linear style)
- Error messages under each field

## Design Specs
- Background: white (#ffffff)
- Input borders: light gray, focus state with subtle ring
- Buttons: solid black or primary blue, rounded corners
- Typography: system fonts (Inter or default sans)
- Spacing: generous whitespace
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UI component primitives</name>
  <files>src/components/ui/Input.tsx, src/components/ui/Button.tsx, src/components/ui/ErrorMessage.tsx, src/components/ui/Card.tsx</files>
  <action>
Create minimal UI component library for auth forms:

**Input.tsx:**
- Props: label, error?, type, placeholder, value, onChange, name
- Style: w-full, px-4 py-3, border rounded-lg, focus:ring-2 focus:ring-black
- Show error state: border-red-500 when error prop provided
- Label positioned above input

**Button.tsx:**
- Props: children, variant ('primary' | 'secondary'), loading?, disabled?, onClick, type
- Primary: bg-black text-white hover:bg-gray-800
- Secondary: bg-transparent border border-gray-300
- Loading state shows spinner

**ErrorMessage.tsx:**
- Props: message
- Style: text-red-600 text-sm mt-1
- Used below inputs

**Card.tsx:**
- Simple container: bg-white, rounded-xl, shadow-sm, p-8
- Max-width: max-w-md for auth forms
- Centered on page

All components use Tailwind (already configured in Next.js init).
  </action>
  <verify>
    <automated>ls src/components/ui/ && grep -l "export" src/components/ui/*.tsx | wc -l</automated>
  </verify>
  <done>UI primitives exist: Input, Button, ErrorMessage, Card; all have proper TypeScript props and Tailwind styling</done>
</task>

<task type="auto">
  <name>Task 2: Create API client and useAuth hook</name>
  <files>src/lib/api-client.ts, src/hooks/useAuth.ts, src/types/auth.ts</files>
  <action>
Create src/lib/api-client.ts:
```typescript
export const api = {
  register: (data: RegisterData) => fetch('/api/auth/register', {...}),
  login: (data: LoginData) => fetch('/api/auth/login', {...}),
  logout: () => fetch('/api/auth/logout', {...}),
  me: () => fetch('/api/auth/me', {...}),
  requestReset: (email: string) => fetch('/api/auth/reset-password', {...}),
  confirmReset: (token: string, newPassword: string) => fetch('/api/auth/reset-password', {...}),
};
```
- All methods return Promise with typed responses
- Handle HTTP errors and return structured error objects
- Include credentials for cookie handling

Create src/hooks/useAuth.ts:
```typescript
export function useAuth() {
  const [user, setUser] = useState<UserSession | null>(null);
  const [loading, setLoading] = useState(true);
  
  useEffect(() => {
    // Check auth on mount via /api/auth/me
  }, []);
  
  const login = async (email, password) => {...};
  const logout = async () => {...};
  const register = async (data) => {...};
  
  return { user, loading, login, logout, register };
}
```

Update src/types/auth.ts with frontend types.
  </action>
  <verify>
    <automated>grep -l "export" src/lib/api-client.ts src/hooks/useAuth.ts && grep "useAuth" src/hooks/useAuth.ts | head -1</automated>
  </verify>
  <done>api-client exports methods for all auth endpoints; useAuth hook manages auth state with user, loading, login, logout, register</done>
</task>

<task type="auto">
  <name>Task 3: Create 3-step registration wizard</name>
  <files>src/components/auth/RegisterWizard.tsx, src/app/auth/register/page.tsx</files>
  <action>
Create src/components/auth/RegisterWizard.tsx:

**Step 1: Email**
- Input: Email address
- Button: "Continuer"
- Validation on submit: check email format, check if already exists (optional)

**Step 2: Password**
- Input: Password (with show/hide toggle)
- Requirements: min 8 chars
- Button: "Continuer"

**Step 3: Profile**
- Input: Name (optional but encouraged)
- Button: "Créer mon compte"
- On submit: call api.register(), redirect to /dashboard on success

**Features:**
- Step indicator (dots or progress)
- Back button to previous step
- All data held in component state
- Error messages under each field on validation failure
- Loading state on submit

Create src/app/auth/register/page.tsx:
- Layout wrapper with Card component
- Render RegisterWizard
- Metadata: title "Créer un compte | Decide"
  </action>
  <verify>
    <automated>grep -E "(Step|Email|Password|Profile)" src/components/auth/RegisterWizard.tsx | head -5</automated>
  </verify>
  <done>RegisterWizard has 3 steps (email, password, profile), step indicator, back navigation, submission validation, field-level errors</done>
</task>

<task type="auto">
  <name>Task 4: Create login form and password reset</name>
  <files>src/components/auth/LoginForm.tsx, src/components/auth/ResetPasswordForm.tsx, src/app/auth/login/page.tsx, src/app/auth/reset-password/page.tsx</files>
  <action>
Create src/components/auth/LoginForm.tsx:
- Email input
- Password input with show/hide
- "Se connecter" button (full width)
- Link: "Mot de passe oublié?" → /auth/reset-password
- Link: "Pas encore de compte? Créer un compte" → /auth/register
- Error display under fields
- On submit: call api.login(), redirect to /dashboard on success

Create src/components/auth/ResetPasswordForm.tsx:
**Request mode (no token in URL):**
- Email input
- "Envoyer le lien" button
- Success message: "Si cet email existe, vous recevrez un lien"

**Confirm mode (token in URL):**
- New password input
- Confirm password input
- "Réinitialiser le mot de passe" button
- On success: redirect to login with success message

Create pages:
- src/app/auth/login/page.tsx - Renders LoginForm
- src/app/auth/reset-password/page.tsx - Reads query param, renders appropriate mode
  </action>
  <verify>
    <automated>grep -l "LoginForm\|ResetPasswordForm" src/components/auth/*.tsx && grep "Mot de passe oublié" src/components/auth/LoginForm.tsx</automated>
  </verify>
  <done>LoginForm has email/password fields, links to register and reset-password; ResetPasswordForm handles request and confirm modes</done>
</task>

<task type="auto">
  <name>Task 5: Create auth layout and dashboard redirect</name>
  <files>src/app/auth/layout.tsx, src/app/dashboard/page.tsx, src/app/page.tsx</files>
  <action>
Create src/app/auth/layout.tsx:
- Minimal layout for auth pages
- Center content vertically and horizontally
- No navigation, no sidebar
- Background: white or very light gray
- Max-width container for form cards

Create src/app/dashboard/page.tsx:
- Simple placeholder: "Tableau de bord - Connecté en tant que [email]"
- Logout button that calls api.logout()
- Protected by middleware (already configured in 01-03)

Update src/app/page.tsx (landing):
- Simple landing with "Commencer" button → /auth/register
- "Se connecter" link → /auth/login
- Brief value proposition text

Verify all routes work:
- / → landing page
- /auth/login → login form
- /auth/register → registration wizard
- /auth/reset-password → reset form
- /dashboard → protected, shows user info or redirects to login
  </action>
  <verify>
    <automated>curl -s http://localhost:3000/auth/login | grep -E "(Se connecter|email)" && curl -s http://localhost:3000/auth/register | grep -E "(Créer|email)"</automated>
  </verify>
  <done>Auth layout centers forms; Dashboard page exists and is protected; Landing page has CTAs to auth flows</done>
</task>

</tasks>

<verification>
- All auth pages render correctly
- Registration wizard flows through 3 steps
- Login form submits and sets cookies
- Password reset flow works end-to-end
- Field-level errors display correctly
- Design is minimalist and clean
</verification>

<success_criteria>
- Can navigate to /auth/register and complete 3-step wizard
- Can log in at /auth/login
- Can request password reset at /auth/reset-password
- Can confirm password reset with token
- Dashboard shows user is authenticated
- Logout clears session and redirects to login
- All forms have field-level error messages
- Design matches Google/Linear minimalist style
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
